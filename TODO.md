# Polling In Run TODO

이 문서는 현재 개발 우선순위와 완료 조건을 관리한다. 장기 방향은
[`README.md`](./README.md), 화면과 기능의 상세 맥락은 [`docs/`](./docs/README.md)를 참고한다.

## 현재 진행 요약

업데이트: 2026-06-24

| 영역 | 진행 상태 | 현재 수준 |
|---|---:|---|
| M1. 지도와 현재 위치 | 9 / 10 | 카카오맵 웹 MVP 완료, iPhone 앱 지도 전략은 Apple MapKit native 우선 |
| M2. 주변 편의시설 | 12 / 12 | 실제 시설 데이터, 지도 영역 기반 조회, 상세 카드, 길찾기, 클러스터링 완료 |
| M3. 러닝 기록 | 5 / 5 | 러닝 상태, 위치 추적, 거리·페이스, 결과 확인, 로컬 기록 저장·조회 완료 |
| M4. 마이 페이지와 로컬 사용자 정보 | 6 / 6 | 로그인 기능은 추후 확장으로 축소, 로컬 프로필·러닝 대시보드·설정 안내 구현 완료 |
| M5. Local-first iPhone 프로토타입 | 7.9 / 8 | Capacitor 설정 완료, Apple MapKit 대체, 로딩 UX, 현위치 이동, 단일 마이 페이지 보강 |
| 기반 작업 | 6 / 6 | React, FastAPI, 테스트, shadcn/ui + Tailwind 기반 완료 |

현재 앱은 카카오맵에서 실제 서울 공원 음수대와 실제 서울 공중화장실을 표시하며, 위치 권한을
허용하면 FastAPI가 현재 지도 영역과 현재 위치 반경 안의 시설을 거리순으로 가공해
반환한다. 마커를 선택하면 주소와 거리 정보를 확인하고 카카오맵 길찾기로 이동할 수 있다.

### 바로 다음 작업

1. iPhone 실기기 홈 화면에서 Apple MapKit 배경 지도 드래그·확대/축소, 현위치 이동, 시설 마커 터치를 확인한다.
2. 기록 페이지 확장 기능의 실제 사용감을 확인하고 필터·정렬·경로 미리보기 표현을 다듬는다.
3. 시설 JSON 로딩 체감을 iPhone에서 확인하고 필요하면 압축·분할 전략을 검토한다.

### 작업 방식

- `main` 직접 커밋은 긴급 수정이나 아주 작은 문서 변경에만 사용한다.
- 기능 작업은 `codex/<작업명>` 브랜치에서 진행하고 GitHub PR로 검토한다.
- PR에는 작업 내용, 테스트/lint 결과, 문서 업데이트 여부, 민감정보 미포함 확인을 적는다.
- 병합 전 `TODO.md`, `README.md`, 관련 `docs/`가 실제 구현 상태와 맞는지 확인한다.

## 현재 목표

### M1. 지도와 현재 위치

- [x] 지도 제공자로 Kakao Maps 선택
- [x] Kakao Developers 앱 생성 및 JavaScript SDK 도메인 등록
- [x] `apps/web/.env.local`에 `VITE_KAKAO_MAP_KEY` 입력
- [x] 카카오맵 SDK 로더 구현
- [x] 홈 화면에 전체 화면 지도 표시
- [x] 현재 위치 권한 요청과 사용자 마커 구현
- [x] 위치 로딩·거부·오류·미지원 상태 처리
- [x] 현재 위치·시설 전체 보기·확대·축소 지도 컨트롤
- [ ] 모바일 브라우저에서 실제 위치 확인
- [x] iPhone 앱용 지도 전략 재선정

현재 확인 사항:

- `http://localhost:5173`과 `http://127.0.0.1:5173`의 SDK 요청과 실제 지도 렌더링을 확인했다.
- 브라우저 위치 권한이 꺼진 상태에서는 위치 안내와 재시도 UI가 표시된다.
- 모바일 브라우저 실기기 확인 절차는 [`모바일 실기기 검증`](./docs/qa/mobile-real-device-check.md)에 준비했다.
- 스마트폰에서 내부 IP로 접속할 때는 해당 URL을 Kakao Developers Web 플랫폼 도메인에 임시 등록해야 한다.
- Capacitor 정적 앱의 `capacitor://localhost` origin에서는 Kakao Maps JavaScript SDK가 Web 플랫폼 도메인 검증에 막힐 수 있다.

완료 조건:

- 사용자가 앱을 열면 지도와 현재 위치를 확인할 수 있다.
- 위치 권한을 거부해도 앱 사용 방법을 안내받을 수 있다.
- API 키는 GitHub에 커밋되지 않는다.

관련 문서:

- [`홈 지도`](./docs/pages/home-map.md)
- [`카카오맵과 현재 위치`](./docs/features/kakao-map-and-location.md)
- [`모바일 실기기 검증`](./docs/qa/mobile-real-device-check.md)

### M2. 주변 편의시설

- [x] 공공 화장실·음수대 데이터 출처 조사
- [x] 시설 데이터 공통 모델 초안 정의
- [x] 샘플 시설 데이터와 FastAPI 정적 시설 API 구현
- [x] 서울 열린데이터광장 API 키 발급 및 `apps/api/.env` 설정
- [x] 서울 음수대 Open API 수집·정규화·캐시 구현
- [x] 공간데이터마켓 화장실 실제 행 데이터 다운로드 승인 및 원본 검증
- [x] FastAPI 현재 위치 반경 시설 API와 거리순 정렬 구현
- [x] 샘플 시설 API를 지도 종류별 마커로 표시
- [x] 실제 서울 음수대 데이터를 지도 마커로 표시
- [x] 시설 필터와 상세 정보 구현
- [x] 화장실 XLSX 컬럼 정의서 확인 및 Git 제외 경로 정리
- [x] 화장실 DBF 원본 수집·정규화·거리순 API 연결
- [x] 현재 지도 영역 기반 시설 조회
- [x] 밀집 지역 마커 클러스터링

완료 조건:

- 현재 위치 주변 시설을 종류별로 구분해 볼 수 있다.
- 외부 데이터 형식은 FastAPI 내부에서 공통 모델로 정규화된다.
- 시설이 많은 지도 영역에서는 가까운 마커를 클러스터로 묶어 표시한다.

관련 문서:

- [`주변 편의시설`](./docs/features/facility-search.md)

### M3. 러닝 기록

- [x] 러닝 시작·일시정지·종료 상태 모델 정의
- [x] 화면이 켜진 상태에서 이동 경로 추적 실험
- [x] 거리와 시간 계산
- [x] 러닝 결과 확인 화면 구현
- [x] 기록과 메모 저장
- [x] 0km 기록 저장 차단과 빈 메모 미저장 처리

완료 조건:

- 사용자가 러닝을 시작하고 종료한 뒤 거리, 시간, 메모를 저장할 수 있다.
- 0km 러닝은 저장하지 않고, 메모가 비어 있으면 기록 데이터에 메모를 남기지 않는다.
- 모바일 웹 백그라운드 위치 추적 한계를 문서화한다.

관련 문서:

- [`러닝 진행`](./docs/pages/running.md)
- [`러닝 결과`](./docs/pages/running-result.md)
- [`러닝 추적`](./docs/features/run-tracking.md)
- [`기록 저장`](./docs/features/record-storage.md)
- [`모바일 실기기 검증`](./docs/qa/mobile-real-device-check.md)

### M4. 마이 페이지와 로컬 사용자 정보

범위:

- [x] 마이 페이지 화면 초안
- [x] 위치 권한과 앱 상태 안내
- [x] 사용자 표시 이름 또는 로컬 프로필 정보 표시
- [x] 로컬 러닝 기록 요약 표시
- [x] 러닝 대시보드 1차 구현: 총 거리, 횟수, 최근 기록, 월간 거리, 최장 거리, 평균 페이스
- [x] 목표 설정과 주간·월간 목표 대비 진행률 표시
- [x] D3 기반 최근 4개월 거리 그래프, 목표 대비 비교 그래프, 이번 달 러닝 날짜 표시
- [x] 앱 설정: 지도 제공자, 데이터 갱신, 로그인/동기화 안내

MVP 제외:

- ID/PW 회원가입과 로그인
- 비밀번호 찾기·재설정
- 이메일·휴대폰 인증
- 소셜 로그인
- 회원 탈퇴
- 관리자 화면

축소 이유:

- 현재 목표는 대외 서비스 운영이 아니라 내 iPhone에서 핵심 러닝 경험을 검증하는 것이다.
- 계정, 비밀번호, 회원 탈퇴는 보안과 운영 책임이 커지므로 local-first MVP에서는 제외한다.
- 러닝 기록은 우선 기기 로컬 저장소에 저장하고, 사용자 정보는 화면 표시용 로컬 프로필로 다룬다.

추후 확장:

- 마이 페이지를 러닝 대시보드로 1차 확장했다.
- 구현 기능: 총 뛴 거리, 러닝 횟수, 최근 러닝, 월간 거리, 최장 러닝 거리, 평균 페이스, 목표 설정, 주간·월간 목표 대비 진행률, D3 월간 거리 그래프, D3 목표 대비 비교 그래프, 달력 기반 러닝 날짜 표시.
- 추가 후보: 평균 페이스 추이 그래프, 목표 대비 비교 그래프 고도화, 기록 리셋, 주간·월간·전체 기간 전환.
- 메모가 있는 러닝만 모아보기는 마이 페이지가 아니라 기록 페이지 확장 후보로 분리한다.
- 대시보드 polish는 추후 작업으로 둔다: 목표 입력 UI 다듬기, 그래프 라벨·색상·간격 조정, 기록이 없을 때 empty state 개선, 기록 리셋 여부 결정, 평균 페이스 추이 그래프 검토.
- 여러 기기에서 기록을 공유해야 할 때 Supabase Auth와 PostgreSQL 저장을 다시 검토한다.
- 공개 서비스로 확장할 때 비밀번호 찾기, 이메일 인증, 계정 삭제 정책을 함께 설계한다.
- 기존 Supabase Auth 실험 코드는 학습 기록과 확장안으로 문서화하되, 핵심 MVP 우선순위에서는 내린다.

완료 조건:

- 사용자가 마이 페이지에서 현재 로컬 프로필과 앱 상태를 확인할 수 있다.
- 로그인하지 않아도 러닝 기록을 저장하고 조회할 수 있다.
- 인증 기능은 추후 확장으로 명확히 분리되어 README와 문서가 혼동을 주지 않는다.

관련 문서:

- [`인증과 설정`](./docs/features/auth-and-settings.md)
- [`마이 페이지`](./docs/pages/my-page.md)

### M5. Local-first iPhone 프로토타입

목표:

- 핵심 러닝 MVP가 갖춰진 뒤 Capacitor로 내 iPhone에서 실제 앱처럼 실행해본다.
- 서버 운영 없이 가능한 기능을 우선 검증한다.
- 앱 환경에서 지도, 현재 위치, 시설 조회, 러닝 추적·기록 흐름이 유지되는지 검증한다.
- 웹 브라우저와 Capacitor WebView의 차이를 문서화한다.

도입 시점:

- 마커 클러스터링과 러닝 시작·일시정지·종료 흐름이 안정화된 뒤 진행한다. 클러스터링은 완료했다.
- 거리와 시간 계산, 러닝 결과 저장 초안이 있어야 아이폰 실기기 테스트의 의미가 커진다.
- Capacitor 설정과 iOS 플랫폼 추가를 시작했다. 실기기 검증은 아직 남아 있다.

범위:

- [x] 핵심 러닝 MVP 이후 `codex/capacitor-ios-preview` 브랜치 생성
- [x] Capacitor 기본 패키지 설치와 설정 파일 추가
- [x] iOS 플랫폼 추가
- [x] `apps/web/dist` 빌드 결과를 Capacitor `webDir`로 연결
- [x] API 호출을 Capacitor 환경에서도 동작하도록 `VITE_API_BASE_URL` 기반으로 정리
- [x] 카카오맵 JavaScript SDK가 iOS WebView origin에서 도메인 제약을 받는 점 확인
- [x] iPhone 앱 지도 대안 선택
- [x] Apple MapKit native 구현 경로 문서화
- [x] NativeMap Capacitor plugin 초안 추가
- [x] WebView overlay의 버튼·카드 영역만 터치받고 나머지는 embedded MapKit으로 넘기는 touch pass-through 구현
- [x] iPhone 홈 화면의 MY 상단 버튼 제거, 브랜드 카드 재배치, native 현위치 이동 버튼 추가
- [ ] iPhone 실기기에서 위치 권한, 현재 위치 마커, 시설 마커 표시 확인
- [x] 시설 데이터 local-first 번들링 전략 1차 구현

주의점:

- Capacitor 앱 안에서는 Vite dev proxy가 없으므로 `/api` 상대 경로만으로 FastAPI에 접근할 수 없다.
- 카카오맵 SDK는 허용 도메인/origin 설정의 영향을 받으며, Capacitor 정적 앱의 `capacitor://localhost`에서는 안정적이지 않다.
- 시설 데이터는 `apps/web/public/data/facilities.json`에 정규화된 JSON으로 생성하며, 프론트는 이 파일을 먼저 조회한다.
- 시설 JSON은 약 5.2MB, gzip 기준 약 389KB이며 로컬 Node 기준 평균 파싱 시간은 약 8.7ms다.
- `NativeMap`은 React가 현재 위치와 표시 중인 시설 최대 300개를 Swift embedded `MKMapView`에 동기화하는 구조로 시작했다.
- `NativeMap.setTouchAreas(...)`는 React UI의 버튼·카드 좌표를 Swift에 넘겨, 그 외 영역의 드래그와 핀치 줌을 native map이 받을 수 있게 한다.
- 실기기 실행에는 Xcode 설정, 개발자 계정 로그인, iPhone 신뢰 설정이 필요할 수 있다.
- 현재 러닝 추적은 화면이 켜지고 앱이 foreground에 있는 동안의 기록을 우선 보장한다.
- 백그라운드 위치 추적은 MVP 범위에서 바로 확정하지 않고, 필요 시 iOS native background location 권한과 별도 플러그인으로 검토한다.

완료 조건:

- 웹 MVP에서 러닝 시작·종료·결과 저장 흐름을 먼저 확인했다.
- Xcode 또는 `npx cap run ios`로 iPhone에서 앱을 실행할 수 있다.
- 앱에서 지도와 현재 위치 권한 흐름을 확인할 수 있다.
- 앱에서 러닝 시작·일시정지·종료 흐름을 확인할 수 있다.
- FastAPI 시설 API 연결 전략을 로컬 개발용과 배포용으로 나눠 문서화한다.
- Capacitor 도입 PR에서 테스트/lint와 민감정보 미포함을 확인한다.

## 이후 목표

- [x] 기록 목록과 상세 화면
- [x] Apple MapKit 기반 지도 전략 검토
- [x] Google Maps SDK/API 비용과 구현 난이도 검토
- [x] 시설 데이터 JSON 번들링
- [x] 마이 페이지 러닝 대시보드 1차 구현: 총 거리, 목표 설정, 달력, D3 그래프
- [x] 기록 페이지 필터와 정렬 1차 구현: 메모 있음, 월별, 거리·시간·페이스 정렬
- [x] 기록 상세 경로 미리보기 1차 구현
- [x] NativeMap Capacitor plugin 초안 추가
- [ ] 기록 페이지 사용감 polish: 필터 레이아웃, 경로 미리보기 라벨, empty state
- [ ] Supabase PostgreSQL 연결은 멀티 디바이스 동기화가 필요해질 때 재검토
- [ ] PWA 설치 경험과 Capacitor 앱 경험 비교
- [ ] 접근성·성능·배포 점검
- [ ] Problem → Solution → Result 포트폴리오 회고

## 완료된 기반 작업

- [x] React + TypeScript + Vite 초기화
- [x] FastAPI 초기화
- [x] React에서 FastAPI health API 호출
- [x] 프론트엔드 테스트·린트·빌드 구성
- [x] 백엔드 테스트 구성
- [x] Tailwind CSS와 shadcn/ui 공용 컴포넌트 기반 설정
